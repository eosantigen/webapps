apiVersion: apps/v1
kind: Deployment
metadata:
  name: tarot
spec:
  selector:
    matchLabels:
      app: tarot
  template:
    metadata:
      labels:
        app: tarot
    spec:
      containers:
      - name: tarot
        image: nginx:latest
        ports:
        - containerPort: 8000
